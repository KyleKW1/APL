// === LOOPS + EXPRESSIONS ===
int i = 1;
while (i <= 5) {
    let square = i ** 2;
    print(square);
    i = i + 1;
}

for (i = 10; i > 5; i = i - 1) {
    let half = i / 2;
    print(half);
}

let negative = -i;
let flipped = !false;

print(negative);
print(flipped);
